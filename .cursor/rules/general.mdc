---
alwaysApply: true
---

After making changes, always run `bun run build` and then `bun test` to make sure the changes work.

e.g.

Testing:

```bash
bun test
```

Building:

```bash
bun run build
```
